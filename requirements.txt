# CoastalVision AI — backend dependencies
# Keep minimal per hackathon scope (Section 22 / 27 of the master spec).

numpy>=1.24
Pillow>=10.0

# Optional, only needed once real satellite raster files (e.g. Sentinel-2
# GeoTIFF/JP2) are plugged in via a rasterio-based loader — not required
# to run the synthetic-sample-data demo.
# rasterio>=1.3

# Optional, for running the automated tests with `pytest` instead of the
# built-in `unittest` (both work — see tests/README or main README).
pytest>=7.0

# Optional, only if area_calculation.py or reporting is extended to use
# pandas/matplotlib for tabular analysis or static plots.
# pandas>=2.0
# matplotlib>=3.7
